<UserControl x:Class="WPFApp.Views.Admin.AdminAccountListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1100">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Account List"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Manage admin, staff, and customer accounts."
                       FontSize="14"
                       Foreground="Gray"/>
        </StackPanel>

        <!-- Filter area -->
        <Border Grid.Row="2"
                Padding="16"
                CornerRadius="10"
                BorderBrush="#DDD"
                BorderThickness="1"
                Background="White">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Keyword -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Search"
                   Margin="0,0,0,6"
                   FontWeight="SemiBold"/>
                        <TextBox x:Name="txtKeyword"
                 Height="36"
                 VerticalContentAlignment="Center"
                 FontSize="14"
                 Padding="10,0"
                 ToolTip="Search by full name, email, or phone"/>

                    </StackPanel>

                    <!-- Role -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Role"
                   Margin="0,0,0,6"
                   FontWeight="SemiBold"/>
                        <ComboBox x:Name="cbRole"
                  Height="36"
                  FontSize="14"
                  SelectedIndex="0">
                            <ComboBoxItem Content="All"/>
                            <ComboBoxItem Content="Admin"/>
                            <ComboBoxItem Content="Staff"/>
                            <ComboBoxItem Content="Customer"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Status -->
                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Status"
                   Margin="0,0,0,6"
                   FontWeight="SemiBold"/>
                        <ComboBox x:Name="cbStatus"
                  Height="36"
                  FontSize="14"
                  SelectedIndex="0">
                            <ComboBoxItem Content="All"/>
                            <ComboBoxItem Content="Active"/>
                            <ComboBoxItem Content="Inactive"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Search button -->
                    <StackPanel Grid.Column="6" VerticalAlignment="Bottom">
                        <TextBlock Text=" "
                   Margin="0,0,0,6"/>
                        <Button x:Name="btnSearch"
                Width="100"
                Height="36"
                Content="Search"
                Click="btnSearch_Click"/>
                    </StackPanel>

                    <!-- Reset button -->
                    <StackPanel Grid.Column="8" VerticalAlignment="Bottom">
                        <TextBlock Text=" "
                   Margin="0,0,0,6"/>
                        <Button x:Name="btnReset"
                Width="100"
                Height="36"
                Content="Reset"
                Click="btnReset_Click"/>
                    </StackPanel>
                </Grid>

                <DataGrid x:Name="dgAccounts"
                          Grid.Row="2"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Margin="0,0,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="220"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="240"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="140"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>

                        <DataGridTemplateColumn Header="Edit" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Edit"
                        Padding="10,4"
                        Click="btnEdit_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action area -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="btnChangeStatus"
                    Content="Change Status"
                    Width="140"
                    Height="38"
                    Click="btnChangeStatus_Click"/>
        </StackPanel>
    </Grid>
</UserControl>