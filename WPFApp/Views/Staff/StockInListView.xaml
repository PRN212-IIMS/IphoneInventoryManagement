<UserControl x:Class="WPFApp.Views.Staff.StockInListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Stock In List"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#19345C"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtSearch"
                     Grid.Column="0"
                     Height="34"
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"
                     ToolTip="Search by stock in id or staff name"/>

            <Button x:Name="btnSearch"
                    Grid.Column="1"
                    Content="Search"
                    Width="90"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="btnSearch_Click"/>

            <Button x:Name="btnReset"
                    Grid.Column="2"
                    Content="Reset"
                    Width="90"
                    Height="34"
                    Margin="0,0,10,0"
                    Click="btnReset_Click"/>

            <Button x:Name="btnCreate"
                    Grid.Column="3"
                    Content="Create Stock In"
                    Width="130"
                    Height="34"
                    Click="btnCreate_Click"/>
        </Grid>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#D9E2EC"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">
            <DataGrid x:Name="dgStockIns"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      RowHeight="36"
                      ColumnHeaderHeight="36"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding StockInId}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Date"
                                        Binding="{Binding StockInDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}"
                                        Width="170"/>

                    <DataGridTextColumn Header="Created By"
                                        Binding="{Binding CreatedByStaff.FullName}"
                                        Width="180"/>

                    <DataGridTextColumn Header="Items"
                                        Binding="{Binding StockInDetails.Count}"
                                        Width="90"/>

                    <DataGridTemplateColumn Header="Actions" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="View Detail"
                                        Width="90"
                                        Height="28"
                                        Click="btnDetail_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>