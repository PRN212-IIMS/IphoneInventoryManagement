<Window x:Class="WPFApp.Views.Staff.OrderDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Order Detail"
        Height="650" Width="980"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F4F7FB">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Order Detail"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#19345C"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#D9E2EC"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="36"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Order ID:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtOrderId" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Order Date:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtOrderDate" Grid.Row="0" Grid.Column="3" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Customer:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtCustomer" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="1" Grid.Column="2" Text="Staff:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtStaff" Grid.Row="1" Grid.Column="3" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Receiver Name:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtReceiverName" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="2" Grid.Column="2" Text="Receiver Phone:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtReceiverPhone" Grid.Row="2" Grid.Column="3" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Status:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtStatus" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center"/>

                <TextBlock Grid.Row="3" Grid.Column="2" Text="Shipping Address:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtShippingAddress" Grid.Row="3" Grid.Column="3" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#D9E2EC"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">
            <DataGrid x:Name="dgDetails"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      RowHeight="36"
                      ColumnHeaderHeight="36">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product ID"
                                        Binding="{Binding ProductId}"
                                        Width="90"/>

                    <DataGridTextColumn Header="Product Name"
                                        Binding="{Binding ProductName}"
                                        Width="240"/>

                    <DataGridTextColumn Header="Quantity"
                                        Binding="{Binding Quantity}"
                                        Width="100"/>

                    <DataGridTextColumn Header="Unit Price"
                                        Binding="{Binding UnitPrice, StringFormat={}{0:N0}}"
                                        Width="140"/>

                    <DataGridTextColumn Header="Line Total"
                                        Binding="{Binding LineTotal, StringFormat={}{0:N0}}"
                                        Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Close"
                    Width="100"
                    Height="34"
                    Click="btnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>